// Tree ADT for multiple Data Types

// Change the typedef for different data types
typedef int infi;

//Greater Sizes
typedef long long INT;

// Node of a Tree
typedef struct node
{
    infi data;
    struct node* left;
    struct node* right;
}node;

//Node pointer as tree
typedef node* tree;

// Functions on Binary Tree
