// Binary Expression Tree ADT for multiple Data Types

// Change the typedef for different data types
typedef char infi;

// Node of a Tree
typedef struct Tnode
{
    infi data;
    struct Tnode* left;
    struct Tnode* right;
}Tnode;

//Greater Sizes
typedef long long INT;

//Node pointer as tree
typedef Tnode* expTree;

// Functions on Binary Expresssion Tree

