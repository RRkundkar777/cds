// Binary Heap for multiple data types

// Change the typedef for different data types
typedef int infi;

// Structure of Binary Heap
typedef struct Aheap
{
    infi *array;
    int size;
    int rear;
}Aheap;

//Greater Sizes
typedef long long INT;

// Heap Pointer
typedef Aheap* heap;

// Functions on AVL Tree